interface:
  display_name: "Hachi UI"
  short_description: "Wireframe-first SVG UI and design systems"
  default_prompt: "Use $hachi-ui to design a wireframe-first app flow, then define its design system and refine it into an editable SVG UI prototype."

policy:
  allow_implicit_invocation: true
