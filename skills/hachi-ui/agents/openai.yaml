interface:
  display_name: "Hachi UI"
  short_description: "Semantic planning to SVG UI prototypes"
  default_prompt: "Use $hachi-ui to translate this product brief into a semantic UI element plan, wireframe the flow, define the design system, and refine it into an editable SVG prototype."

policy:
  allow_implicit_invocation: true
